# Real-Time Job Sync - Visual Flow Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                     MOBILE APP ↔ WEBAPP REAL-TIME SYNC                  │
└─────────────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│              │         │              │         │              │
│  Mobile App  │   →     │  Firestore   │   →     │   Webapp     │
│ (Staff User) │  writes │   Database   │ pushes  │   (Admin)    │
│              │         │              │         │              │
└──────────────┘         └──────────────┘         └──────────────┘
      │                        │                         │
      │                        │                         │
    Staff                   Real-time                  UI Auto
   Actions                  Listeners                  Updates
```

## Data Flow - Job Status Update

```
STEP 1: Mobile Staff Action
────────────────────────────────────────────────────────────
┌─────────────────┐
│  Mobile App     │
│                 │
│  Staff sees:    │
│  📋 Job Card    │
│  "Clean Villa"  │
│                 │
│  [ Accept Job ] │ ← Staff taps this button
└────────┬────────┘
         │
         │ Calls mobile service
         │
         ▼


STEP 2: Firestore Update
────────────────────────────────────────────────────────────
┌─────────────────────────────────────┐
│         Firestore Database          │
│                                     │
│  Collection: jobs                   │
│  Document: abc123                   │
│                                     │
│  BEFORE:                            │
│  {                                  │
│    status: "assigned"      ◄────────┼─── Old value
│    assignedTo: "staffId",          │
│    ...                             │
│  }                                 │
│                                    │
│  AFTER UPDATE:                     │
│  {                                 │
│    status: "accepted"     ◄────────┼─── New value ✨
│    acceptedAt: Timestamp  ◄────────┼─── Added ✨
│    updatedAt: Timestamp   ◄────────┼─── Updated ✨
│    assignedTo: "staffId",         │
│    ...                            │
│  }                                │
└────────┬──────────────────────────┘
         │
         │ onSnapshot triggered
         │
         ▼


STEP 3: Webapp Real-Time Listener
────────────────────────────────────────────────────────────
┌─────────────────────────────────────┐
│  RealtimeJobSyncService             │
│                                     │
│  onSnapshot(jobsRef, (snapshot) => {│
│    snapshot.docChanges().forEach    │
│                                     │
│    Detected: MODIFIED              │
│    Job ID: abc123                  │
│    Previous: "assigned"            │
│    Current: "accepted"             │
│                                    │
│    🔔 Trigger callbacks            │
│  })                                │
└────────┬──────────────────────────┘
         │
         │ Notify React hooks
         │
         ▼


STEP 4: React Hook Update
────────────────────────────────────────────────────────────
┌─────────────────────────────────────┐
│  useAllJobs() Hook                  │
│                                     │
│  State update triggered:            │
│  setJobs(updatedJobs)              │
│                                    │
│  Callbacks fired:                  │
│  - onJobStatusChange()             │
│  - toast.success()                 │
│                                    │
│  Component re-render scheduled     │
└────────┬──────────────────────────┘
         │
         │ React re-renders
         │
         ▼


STEP 5: UI Update
────────────────────────────────────────────────────────────
┌─────────────────────────────────────┐
│  React Component (Webapp)           │
│                                     │
│  BEFORE:                           │
│  ┌──────────────────────┐         │
│  │ 📋 Clean Villa       │         │
│  │ Status: Assigned     │         │
│  │ [Blue Badge]         │         │
│  └──────────────────────┘         │
│                                    │
│  AFTER (200-500ms later):         │
│  ┌──────────────────────┐         │
│  │ 📋 Clean Villa       │         │
│  │ Status: Accepted ✅  │         │
│  │ [Green Badge] 🟢     │ ← Updated!
│  └──────────────────────┘         │
│                                    │
│  Toast notification:               │
│  "✅ Staff accepted Clean Villa"  │
└─────────────────────────────────────┘

Total time: ~200-500ms from button tap to UI update!
```

## Component Integration Flow

```
YOUR COMPONENT                      HOOK                    SERVICE
────────────────────────────────────────────────────────────────────────

function MyPage() {                useAllJobs()            RealtimeJobSync
                                      │                          │
  const { jobs } = useAllJobs(); ────┤                          │
                    │                 │                          │
                    │                 │                          │
                    │            useEffect() ────────────────────┤
                    │                 │                          │
                    │                 │                     subscribeToAllJobs()
                    │                 │                          │
                    │                 │                     onSnapshot(...)
                    │                 │                          │
                    │                 │                          │
  return (          │                 │                          │
    <div>           │                 │                    [Listening...]
      {jobs.map...} │                 │                          │
    </div>          │                 │                          │
  )                 │                 │                          │
}                   │                 │                          │
                    │                 │                     Change detected!
                    │                 │                          │
                    │            setJobs(new) ◄──────────────────┤
                    │                 │                          
                    │                 │                          
                ◄───┴─────────────────┘                          
           [Re-render with                                       
            updated jobs]                                        
```

## Real-World Timeline

```
TIME      MOBILE APP              FIRESTORE           WEBAPP
────────────────────────────────────────────────────────────────
00:00ms   Staff taps              
          "Accept Job"            
          │                       
          ▼                       
00:10ms   API call sent           
          │                       
          ▼                       
00:50ms                           Write received
                                  Data updated
                                  │
                                  ▼
00:80ms                           Listeners notified
                                  │
                                  ▼
00:120ms                                          onSnapshot fires
                                                  │
                                                  ▼
00:150ms                                          Hook updates state
                                                  │
                                                  ▼
00:200ms                                          Component re-renders
                                                  Badge turns GREEN ✅
                                                  Toast appears
                                                  
TOTAL: ~200ms from tap to webapp update
```

## Status Badges Visual

```
┌────────────────────────────────────────────────────────────┐
│                    Job Status Badges                       │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  📋 Assigned      →   ✅ Accepted    →   🚀 In Progress   │
│  ┌──────────┐        ┌──────────┐       ┌──────────┐    │
│  │ Assigned │        │ Accepted │       │In Progress│    │
│  └──────────┘        └──────────┘       └──────────┘    │
│   [Blue]             [Green]            [Orange]         │
│                                                           │
│                                         ↓                 │
│                                                           │
│                      ✔️ Completed                        │
│                      ┌──────────┐                        │
│                      │Completed │                        │
│                      └──────────┘                        │
│                      [Emerald]                           │
│                      (Moved to completed_jobs)           │
└───────────────────────────────────────────────────────────┘
```

## Hook Usage Patterns

```
PATTERN 1: Simple List
────────────────────────────────────────────────────────────
const { jobs } = useAllJobs();

return jobs.map(job => <JobCard job={job} />);
                         │
                         └─ Updates automatically when
                            mobile app changes status


PATTERN 2: With Notifications
────────────────────────────────────────────────────────────
const { jobs } = useAllJobs({
  showNotifications: true  ← Enables toast messages
});

When status changes:
┌────────────────────────┐
│ ✅ Staff accepted job! │ ← Auto-appears
└────────────────────────┘


PATTERN 3: With Callback
────────────────────────────────────────────────────────────
const { jobs } = useAllJobs({
  onJobStatusChange: (job, prevStatus) => {
    if (job.status === 'in_progress') {
      startTimer(job.id);    ← Custom logic
      sendEmail(job);         ← Custom notifications
      updateDashboard();      ← Custom updates
    }
  }
});


PATTERN 4: Filtered by Property
────────────────────────────────────────────────────────────
const { jobs } = usePropertyJobs(propertyId);
                                    │
                                    └─ Only jobs for this property


PATTERN 5: Real-Time Stats
────────────────────────────────────────────────────────────
const { stats } = useJobStats();

stats = {
  total: 10,      ← Updates live
  assigned: 3,    ← Updates live
  accepted: 2,    ← Updates live
  inProgress: 4,  ← Updates live
  completed: 1    ← Updates live
}
```

## Error Handling Flow

```
┌─────────────────┐
│  Network Error  │
│  or            │
│  Firebase Down  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  onSnapshot     │
│  error callback │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Hook catches   │
│  setError(err)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Component      │
│  if (error)     │
│    show error   │
└─────────────────┘
```

## Cleanup Flow

```
Component Mounts
      │
      ▼
useEffect runs
      │
      ├─ Subscribe to Firestore
      │  (onSnapshot listener created)
      │
      └─ Return cleanup function
            │
            │ Component unmounts
            │
            ▼
      Cleanup function runs
            │
            └─ unsubscribe()
               (Listener removed)
               (Memory freed)
```

## Multi-Tab Sync

```
TAB 1                    FIRESTORE               TAB 2
────────────────────────────────────────────────────────────
Shows job as            
"Assigned"              
(Blue badge)            

Mobile staff                        
accepts job             

                        Update written
                        │
                        ├──► onSnapshot fires    
                        │                        
                        └──────────────────────► onSnapshot fires

Badge turns             
GREEN ✅                                         Badge turns
                                                GREEN ✅

Both tabs update simultaneously!
No communication between tabs needed!
Firestore handles synchronization!
```

## Summary

```
┌───────────────────────────────────────────────────────────────┐
│                                                               │
│  ONE HOOK = REAL-TIME JOB UPDATES                            │
│                                                               │
│  const { jobs } = useAllJobs();                              │
│                                                               │
│  ✅ Automatic updates when mobile app changes status         │
│  ✅ Toast notifications when jobs change                     │
│  ✅ Status badges with live indicators                       │
│  ✅ Automatic cleanup on unmount                             │
│  ✅ Error handling built-in                                  │
│  ✅ TypeScript types for autocomplete                        │
│  ✅ ~200ms latency from mobile to webapp                     │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```
