rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ===== DEVELOPMENT MODE: COMPLETELY OPEN RULES =====
    // WARNING: These rules are for development only!
    // ALLOWS ALL ACCESS - EVEN UNAUTHENTICATED
    
    // Allow all access (including unauthenticated) - FOR DEVELOPMENT ONLY
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
